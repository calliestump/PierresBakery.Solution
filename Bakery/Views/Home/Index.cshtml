@{
    Layout = "_Layout";
}

@using Bakery.Models;

<div class="container card">
  <br />
  <input class="btn btn-info" type="button" value="Pierre's Flavors" onclick='location.href="@Url.Action("Index", "Flavors")";' />
  <input class="btn btn-info" type="button" value="Pierre's Treats" onclick='location.href="@Url.Action("Index", "Treats")";' />
  <br /><br />
</div>