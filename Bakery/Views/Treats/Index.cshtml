@{
  Layout = "_Layout";
}

@using Bakery.Models;
@model List<Bakery.Models.Treat>;
<div class="container card">
<h3>Pierre's Treats</h3>
<hr />
@if (Model.Count == 0)
{
  <h4><span>No treats have been added to Pierres :(</span></h4> // This will not show on views. Unsure if it is a syntax or VS error.
}

@foreach (Treat treat in Model)
{
  <h4>@Html.ActionLink($"{treat.TreatName}", "Details", new { id = treat.TreatId })</h4>
}
<br />
<p>@Html.ActionLink("Add new treat", "Create")</p>
</div>